build:
  version: "3.0"
accounts:
  message: "Welcome to the Accounts website"
  contactDetails:
    name: "sabja"
    phone: "293848929"
    email: "sabja@gmail.com"
eureka:
  instance:
    preferIpAddress: true
  client:
    fetchregistry: true
    registerWithEureka: true
  serviceUrl:
    defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
